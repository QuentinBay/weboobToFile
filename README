# weboobToFile

weboobToFile is a module to serialize results from Weboob Boobank application into a single file.

## To run this module
'./run.py'	[in root directory]

## To test this module
'py.test'	[in tests directory]
